<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        .student {
            display: flex;
            flex-direction: row;
            box-shadow: 0px 0px 5px black;
            border-radius: 10px;
            margin: 5px;
            padding: 5px;
        }

        .student p {
            font-size: 20px;
        }

        .pic {
            border: 1px solid grey;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            margin-right: 15px;
        }

        .btn {
            all: unset;
            width: 320px;
            margin-left: 10px;
        }

        .id {
            margin-left: 20px;
        }

        .buttons {
            display: flex;
            flex-direction: row;
            margin-top: 10px;
        }

        .buttons button {
            margin-right: 10px;
            font-size: 18px;
            padding: 10px;
            margin-left: 15px;
        }
    </style>
</head>

<body>
    <div class="main">
        <h1>Basic Classes</h1>
        <div class="box">
            <% for(stud of stu){ %>
                <form action="/student/<%=stud._id%>" method="get">
                    <button class="btn">
                        <div class="student">
                            <div class="pic"></div>
                            <B>
                                <p>
                                    <%=stud.studentName%>
                                </p>
                            </B>
                            <div class="id">
                                <%=stud.id%>
                            </div>
                        </div>
                    </button>
                </form>
                <% } %>
        </div>
        <div class="buttons">
            <form action="/admissionpass" method="get">
                <button>New admission</button>
            </form>
            <form action="/payfee" method="get">
                <button>Paymet Fee</button>
            </form>
        </div>


    </div>


</body>

</html>